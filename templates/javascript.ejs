<%_ function printCall(route, inputs) { _%>
function(<%= Object.keys(inputs).join(', ') %>, cb) {
    _rpc_("<%= route[0] %>", "<%= route[1] %>", { <%= Object.keys(inputs).map(function(key) { return key + ": " + key }).join(', ') %> }, cb);
}
<%_ } _%>

<%_
    routers.forEach(function(router) {
        router.calls.forEach(function(call) {
            call.routes.forEach(function(route) {
_%>
<%= printCall(route, call.inputs) %>
<%_
            });
        });
    });
_%>